<h2>Latest post:</h2>

<% @results.each do |post| %>
	<p>
		<%= post['content'] %>
		<br />
		added at <%= post['created_date'] %>
		<br/>
		<a href="/post/<%= post['id'] %>">Comments</a>
	</p>
	<hr />
<% end %>